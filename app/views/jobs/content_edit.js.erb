$("article.content").html('<%= j render("steps/#{@step}Edit") %>')
$("#job_edit_link").hide()



// / render 'jobs/content_edit', locals: {step: params[:step]}


// / = render @job

// / - content_for :content do
// /   = render "steps/#{step}Edit"
